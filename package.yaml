<<: !include ../package-yaml-support/base-package.yaml
name: http
synopsis: Make Elm style HTTP requests
extra-source-files:
- README.md
library:
  dependencies:
  - aeson
  - base
  - bytestring
  - conduit
  - http-client
  - http-client-tls
  - http-types
  - nri-prelude
  - text
  exposed-modules:
  - Http
  source-dirs: src

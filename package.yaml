<<: !include ../package-yaml-support/base-package.yaml
name: redis
synopsis: Redis helper for NRI code
library:
  dependencies: &dependencies
  - aeson
  - async
  - bytestring
  - base
  - nri-prelude
  - conduit
  - env-parser
  - safe-exceptions
  - health
  - hedis
  - resourcet
  - text
  - unordered-containers
  exposed-modules:
  - Redis
  - Redis.Text
  - Redis.Json
  - Redis.ByteString
  - Redis.Mock
  source-dirs:
  - src
tests:
  spec:
    dependencies: *dependencies
    main: Main.hs
    source-dirs:
      - test/spec
      - src
